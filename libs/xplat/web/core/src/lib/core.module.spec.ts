describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { DeadCoreModule } from './core.module';

describe('DeadCoreModule', () => {
  it('should work', () => {
    expect(new DeadCoreModule(null)).toBeDefined();
  });
});
*/
